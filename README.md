# vimrc
my custom vimrc

## installation
to install simply place all folders and files in this repo to ~/.vim/ in a addition to adding a directory called ~/.vim/undodir/<br>
after installing plug.vim, enter vim and run <code>:PlugInstall</code>
